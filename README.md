# induction-thermal-optimizer

Self-optimizing pipeline for induction-heated tip design:

OpenSCAD (parametric geometry) -> FEMM (induction power deposition) -> OpenFOAM (thermal) -> Optuna (optimization).
